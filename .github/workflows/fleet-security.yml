# Fleet security lint/scan — self-hosted. Badge reflects honest job status on trunk.
name: fleet-security

on:
  push:
    branches: [main, master, dev]
  pull_request:
    branches: [main, master, dev]
  schedule:
    - cron: "17 7 * * 1"
  workflow_dispatch:

concurrency:
  group: fleet-security-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  gitleaks:
    name: gitleaks
    runs-on: [self-hosted, linux, x64, podman]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Run gitleaks
        run: |
          set -euo pipefail
          if command -v gitleaks >/dev/null 2>&1; then
            gitleaks detect --source . --verbose --redact
          elif command -v docker >/dev/null 2>&1; then
            docker run --rm -v "$PWD:/path:ro" zricethezav/gitleaks:latest detect --source=/path --verbose --redact
          elif command -v podman >/dev/null 2>&1; then
            podman run --rm -v "$PWD:/path:ro" docker.io/zricethezav/gitleaks:latest detect --source=/path --verbose --redact
          else
            echo "gitleaks/docker/podman not found globally. Downloading gitleaks binary..."
            curl -sSLo gitleaks.tar.gz "https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/gitleaks_8.30.1_linux_x64.tar.gz"
            tar -xzf gitleaks.tar.gz gitleaks
            chmod +x ./gitleaks
            ./gitleaks detect --source . --verbose --redact
          fi

  trivy-fs:
    name: trivy filesystem (vuln+secret+license)
    runs-on: [self-hosted, linux, x64, podman]
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Trivy fs
        run: |
          set -euo pipefail
          if ! command -v trivy >/dev/null 2>&1; then
            echo "trivy missing — advisory skip (continue-on-error)"
            exit 0
          fi
          trivy fs --scanners vuln,secret,license \
            --severity HIGH,CRITICAL \
            --skip-dirs .git --skip-dirs target --skip-dirs node_modules --skip-dirs .venv \
            --exit-code 1 \
            .
